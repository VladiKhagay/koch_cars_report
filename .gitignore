node_modules/
dist/
.wrangler/
*.log
.DS_Store

# Environment files with real secrets — never commit these.
.env
.env.local
.env.*.local

# Backup artifacts if generated locally
backup.sql.gz

# /design-sync (claude.ai/design)
web/.ds-sync/
web/ds-bundle/
web/.design-sync/.cache/
web/.design-sync/learnings/
web/.design-sync/node_modules
# build artifact, regenerated by the sync's buildCmd
web/.design-sync/compiled.css

# Local Claude Code config: vendored skills, machine-specific paths,
# and per-user permissions. Nothing here is needed to build or deploy.
# Drop this line if you decide the team should share it.
.claude/

# Generated knowledge graph (graphify) — rebuildable, 1.3M with cache
graphify-out/
